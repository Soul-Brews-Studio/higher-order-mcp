# Verification — 2026-09-02 (commit bfc00ec + follow-ups)

All checks below were run against the real edge deployment `https://homcp.laris.workers.dev` (account `a5eabdc2…`, Worker `homcp`) unless stated.

## Local

| Check | Result |
|---|---|
| `npm run check` | name guard ok (38 files) · `tsc --noEmit` clean · vitest 13 files, 209 tests passed |

## Edge — protocol and auth

| Check | Result |
|---|---|
| `GET /health` | `{"ok":true,"name":"homcp","schema":"ok","oauth":{"cimd":true,"dcr":true},"tools":{"visible":15,"total":22}}` |
| `GET /.well-known/oauth-authorization-server` | `client_id_metadata_document_supported: true`, `none` auth method, `S256`, `registration_endpoint` |
| `GET /.well-known/oauth-protected-resource/mcp` | `resource == https://homcp.laris.workers.dev/mcp` |
| `POST /mcp` no bearer | `401` + `WWW-Authenticate: Bearer realm="OAuth", resource_metadata="…/.well-known/oauth-protected-resource/mcp"` |
| Static bearer `tools/list` | 15 default tools, sorted |
| `GET /sse` | `410` |
| Landing `/` | prints `claude mcp add --transport http --scope user homcp https://homcp.laris.workers.dev/mcp` and the claude.ai prefill link |

## Claude Code 2.1.257

1. `claude mcp add --transport http --scope user homcp https://homcp.laris.workers.dev/mcp`
2. `claude mcp login homcp --no-browser` (inside tmux) → URL with `client_id=https://claude.ai/oauth/claude-code-client-metadata` (CIMD) and a random loopback port → consent page ("Connect Claude Code to homcp", label `claude-code`) approved with the owner passphrase → "Authenticated with homcp" → `claude mcp get homcp` = Connected.
3. Real session (`claude -p --model sonnet --allowedTools "mcp__homcp__*"`): `whoami` → `via: oauth`, `clientKey: claude-code`; `define_tool greet` (template kind) → `call_tool greet {who:"Nat"}` → `Hello, Nat! Served by homcp.` → `promote_tool greet` → "promoted, scope deploy, catalogVersion 3".

## claude.ai (Max plan)

1. Customize → Connectors → Add → Add custom connector → Name `homcp`, URL `…/mcp` → Continue: step 2 auto-detected "Always required" and CIMD ("Detected") → Add → Connect → consent → Connected.
2. After the Claude Code session promoted `greet`: connector menu → **Refresh tools list** → tool permissions now list 16 tools grouped "Read-only tools" (Describe tool, greet, List tools, Memory stats, Read memory, Recall memories, Server info, Who am I) and "Write/delete tools" (Call tool, Define tool, Demote tool, Forget memory, Promote tool, …). Grouping follows the tool annotations.

## Deploy to Cloudflare button (public repo `Soul-Brews-Studio/higher-order-mcp`)

Setup page read the template correctly: KV `OAUTH_KV` (new), D1 `DB`, secret `OWNER_PASSPHRASE`, var `MCP_SERVER_NAME`, deploy command auto-detected as `npm run deploy`, build command empty. Filled: project `homcp-button-test`, Git account (the account's connected GitHub org), passphrase, `MCP_SERVER_NAME=button-test`.

Workers Builds log: clone seed repo → push to the new GitHub repo → `npm clean-install` (node 24.18, npm 10.9) → `npm run deploy`. Result:

| Check | Result |
|---|---|
| `GET https://homcp-button-test.laris.workers.dev/health` | `{"ok":true,"name":"button-test","schema":"ok","oauth":{"cimd":true,"dcr":true}}` |
| AS metadata | CIMD `true`, issuer = the new host |
| Landing | `claude mcp add --transport http --scope user button-test https://homcp-button-test.laris.workers.dev/mcp` |
| `POST /mcp` no bearer | `401` |

Two setup-page gotchas found (now documented in README):

- The D1 selector **pre-selects an existing database whose name matches `database_name`** (here the live `homcp` DB). Pick "+ Create new" unless you intend to share data.
- The secret prompt is **pre-filled with whatever `.env.example` contains**. The template now ships `OWNER_PASSPHRASE=` empty so a placeholder can never become the real secret.

The throwaway Worker, KV namespace and GitHub repo were deleted after this run.
