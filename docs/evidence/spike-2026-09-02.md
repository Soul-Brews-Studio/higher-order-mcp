# Spike evidence — 2026-09-02

Minimal Worker (`src/index.ts` at commit "spike") wired as:
`OAuthProvider` (workers-oauth-provider 0.10.3, DCR + CIMD) → `apiRoute /mcp` → `createMcpHandler` (agents 0.22.0, stateless, legacy lane) → fresh `McpServer` per request; Hono for `/`, `/health`, `/authorize`; D1 `DB`; KV `OAUTH_KV`; optional static bearer `MCP_API_TOKEN` checked before the provider.

## Local (wrangler dev, port 8799)

| Check | Result |
|---|---|
| `GET /.well-known/oauth-authorization-server` | `client_id_metadata_document_supported: true`, `token_endpoint_auth_methods_supported` includes `none`, `code_challenge_methods_supported: ["S256"]`, `registration_endpoint` present |
| `GET /.well-known/oauth-protected-resource` and `/mcp` | `resource` equals the typed URL (`…/mcp`) |
| `POST /mcp` without token | `401` + `WWW-Authenticate: Bearer realm="OAuth", resource_metadata="…/.well-known/oauth-protected-resource/mcp"` |
| `GET /mcp` | `405` |
| Static bearer, legacy `initialize` (2025-06-18) | `200`, SSE frame, no `Mcp-Session-Id`; `tools/list` and `tools/call` work without a session; D1 read inside the tool works |
| Modern lane `server/discover` (2026-07-28) | `200` JSON, `supportedVersions:["2026-07-28"]`, `cacheScope:"private"` |
| DCR → PKCE authorize → wrong passphrase | `201` → form → `403` |
| Right passphrase → token → MCP call | `302` with `code` + `iss` → `200` access+refresh token with `resource` → `tools/call whoami` returns `props.via = "oauth"` |
| CIMD `/authorize` (`client_id=https://claude.ai/oauth/mcp-oauth-client-metadata`) | `200` consent form (provider fetched Anthropic's document) |

## Edge (https://homcp.laris.workers.dev)

- `wrangler deploy` auto-provisioned `OAUTH_KV` and D1 `homcp` from id-less bindings; no ids written back.
- Same metadata / 401 / static-token results as local.
- CIMD `/authorize` from the edge: `200` (Anthropic's document fetched from a Cloudflare Worker without a challenge).
- Claude Code 2.1.257: `claude mcp add --transport http --scope user homcp-spike …/mcp` then `claude mcp login homcp-spike --no-browser` (in tmux) → Claude Code chose CIMD (`client_id=https://claude.ai/oauth/claude-code-client-metadata`, `redirect_uri=http://localhost:3118/callback`) → consent approved in ego-browser → "Authenticated with homcp-spike" → status Connected. Remote KV: zero `client:` keys (no DCR), one `grant:`, one `token:`.
- claude.ai: Add custom connector "homcp" → Step 2 auto-detected "Always required" + CIMD → Add → Connect → consent → connector shows the `Who am I` tool under Tool permissions. Connector id `864fc0e8-9217-4474-81ee-7c88ab4f4e6e`.
- Trap: this Cloudflare account exceeded D1's free-tier daily row reads (code 7500) so remote migrations/queries failed until 00:00 UTC.
